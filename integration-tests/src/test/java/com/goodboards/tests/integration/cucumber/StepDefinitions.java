package com.goodboards.tests.integration.cucumber;

import client.ClientUtil;
import io.cucumber.java.en.Given;
import io.ktor.client.*;
import io.ktor.client.engine.cio.*;
import io.ktor.client.request.*;
import io.ktor.client.statement.*;

public class StepDefinitions {
    @Given("form page should have the top level text")
    public void getFormPageAndCheckTopLevelText() {
        // ApiClient initialization
        // Make the call
        // Get the HTML response
        // Parse and find the element.

    }
}